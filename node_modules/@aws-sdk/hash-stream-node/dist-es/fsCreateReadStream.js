import { createReadStream } from "fs";
export var fsCreateReadStream = function (readStream) {
    return createReadStream(readStream.path, {
        fd: readStream.fd,
        start: readStream.start,
        end: readStream.end,
    });
};
